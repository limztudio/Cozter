"""Built-in agent tools — the core toolkit shipped with the bot.

Drop a new ``.py`` file in this directory only if it should ship with
the bot as part of the baseline. User-added tools belong in the
sibling ``plugins/`` directory instead.
"""
